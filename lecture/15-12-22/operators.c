#include <stdio.h>
int main()
{
    int a;
    int b;
    int c=a+b;
    printf("A value:");
    scanf("%d", &a);

    printf("B value:");
    scanf("%d", &b);

    printf("C value:=%d",c);
    scanf("%d", &c);

    printf("c value is%d",c);

    return 0;
}
